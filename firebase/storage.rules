rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // File uploads are not required by BRMS Version 1.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
